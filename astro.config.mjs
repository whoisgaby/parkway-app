// @ts-check
import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://whoisgaby.github.io",
  base: "/parkway-app/",
  output: "static",
  base: "/parkway-app/",
});
